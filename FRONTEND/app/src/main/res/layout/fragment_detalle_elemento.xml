<?xml version="1.0" encoding="utf-8"?>
<layout xmlns:android="http://schemas.android.com/apk/res/android"
    xmlns:app="http://schemas.android.com/apk/res-auto">

    <data />

    <androidx.constraintlayout.widget.ConstraintLayout
        android:layout_width="match_parent"
        android:layout_height="wrap_content"
        android:background="@android:color/transparent"
        android:padding="16dp">

        <!-- Botón cerrar -->
        <ImageButton
            android:id="@+id/btnCerrar"
            android:layout_width="36dp"
            android:layout_height="36dp"
            android:background="?attr/selectableItemBackgroundBorderless"
            android:src="@android:drawable/ic_menu_close_clear_cancel"
            android:tint="?android:textColorSecondary"
            app:layout_constraintTop_toTopOf="parent"
            app:layout_constraintEnd_toEndOf="parent" />

        <!-- Título centrado -->
        <TextView
            android:id="@+id/tvTitulo"
            android:layout_width="0dp"
            android:layout_height="wrap_content"
            android:text="Detalle del Elemento"
            android:textSize="20sp"
            android:textStyle="bold"
            android:textColor="#222"
            android:gravity="center"
            app:layout_constraintTop_toTopOf="parent"
            app:layout_constraintStart_toStartOf="parent"
            app:layout_constraintEnd_toEndOf="parent"
            android:layout_marginTop="12dp"
            app:layout_constraintBottom_toTopOf="@id/cardContent" />

        <!-- Contenido dentro de CardView -->
        <androidx.cardview.widget.CardView
            android:id="@+id/cardContent"
            android:layout_width="0dp"
            android:layout_height="wrap_content"
            app:cardCornerRadius="20dp"
            app:cardElevation="12dp"
            app:layout_constraintTop_toBottomOf="@id/tvTitulo"
            app:layout_constraintStart_toStartOf="parent"
            app:layout_constraintEnd_toEndOf="parent"
            app:layout_constraintBottom_toBottomOf="parent"
            android:layout_marginTop="16dp">

            <LinearLayout
                android:layout_width="match_parent"
                android:layout_height="wrap_content"
                android:orientation="vertical"
                android:padding="16dp"
                android:background="@android:color/white">

                <!-- Incluidos reutilizables -->
                <include android:id="@+id/atributoCodigo" layout="@layout/item_detalle_atributo" />
                <include android:id="@+id/atributoDescripcion" layout="@layout/item_detalle_atributo" />
                <include android:id="@+id/atributoMarca" layout="@layout/item_detalle_atributo" />
                <include android:id="@+id/atributoModelo" layout="@layout/item_detalle_atributo" />
                <include android:id="@+id/atributoColor" layout="@layout/item_detalle_atributo" />
                <include android:id="@+id/atributoEstadoFisico" layout="@layout/item_detalle_atributo" />
                <include android:id="@+id/atributoEstadoSistema" layout="@layout/item_detalle_atributo" />
            </LinearLayout>
        </androidx.cardview.widget.CardView>
    </androidx.constraintlayout.widget.ConstraintLayout>
</layout>




