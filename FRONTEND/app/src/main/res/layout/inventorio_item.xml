<?xml version="1.0" encoding="utf-8"?>
<com.google.android.material.card.MaterialCardView
    xmlns:android="http://schemas.android.com/apk/res/android"
    xmlns:app="http://schemas.android.com/apk/res-auto"
    xmlns:tools="http://schemas.android.com/tools"
    android:layout_width="match_parent"
    android:layout_height="wrap_content"
    android:layout_marginHorizontal="16dp"
    android:layout_marginTop="8dp"
    android:layout_marginBottom="8dp"
    app:cardCornerRadius="8dp"
    app:cardElevation="2dp">

    <androidx.constraintlayout.widget.ConstraintLayout
        android:layout_width="match_parent"
        android:layout_height="wrap_content"
        android:padding="16dp">

        <TextView
            android:id="@+id/inventory_id_text_view"
            android:layout_width="wrap_content"
            android:layout_height="wrap_content"
            android:contentDescription="Identificador de inventario"
            android:textColor="?android:attr/textColorSecondary"
            android:textSize="12sp"
            app:layout_constraintStart_toStartOf="parent"
            app:layout_constraintTop_toTopOf="parent"
            tools:text="ID: 123" />

        <TextView
            android:id="@+id/inventory_description_text_view"
            android:layout_width="0dp"
            android:layout_height="wrap_content"
            android:layout_marginTop="8dp"
            android:layout_marginEnd="8dp"
            android:textColor="?attr/colorOnSurface"
            android:textSize="18sp"
            android:textStyle="bold"
            app:layout_constraintEnd_toStartOf="@+id/button_toggle_actions"
            app:layout_constraintStart_toStartOf="parent"
            app:layout_constraintTop_toBottomOf="@id/inventory_id_text_view"
            tools:text="Inventario de Suministros de Oficina con un nombre muy largo para probar el ajuste de texto" />

        <TextView
            android:id="@+id/inventory_elements_text_view"
            android:layout_width="wrap_content"
            android:layout_height="wrap_content"
            android:layout_marginTop="8dp"
            android:contentDescription="Número de elementos en el inventario"
            android:textColor="?android:attr/textColorSecondary"
            android:textSize="14sp"
            app:layout_constraintStart_toStartOf="parent"
            app:layout_constraintTop_toBottomOf="@id/inventory_description_text_view"
            tools:text="Elementos: 50" />

        <ImageButton
            android:id="@+id/button_toggle_actions"
            android:layout_width="40dp"
            android:layout_height="40dp"
            android:background="?attr/selectableItemBackgroundBorderless"
            android:contentDescription="Mostrar/Ocultar acciones"
            android:src="@drawable/expand_all_24px"
            android:scaleType="fitCenter"
            android:padding="8dp"
            app:layout_constraintTop_toTopOf="parent"
            app:layout_constraintEnd_toEndOf="parent"
            app:tint="@color/dark_gray"
            tools:ignore="ImageContrastCheck" />

        <LinearLayout
            android:id="@+id/action_buttons_container"
            android:layout_width="0dp"
            android:layout_height="wrap_content"
            android:orientation="horizontal"
            android:layout_marginTop="16dp"
            android:gravity="end"
            android:visibility="gone"
            app:layout_constraintTop_toBottomOf="@id/inventory_elements_text_view"
            app:layout_constraintStart_toStartOf="parent"
            app:layout_constraintEnd_toEndOf="parent"
            app:layout_constraintBottom_toBottomOf="parent">

        <ImageButton
            android:id="@+id/button_edit_inventory"
            android:layout_width="48dp"
            android:layout_height="48dp"
            android:background="?attr/selectableItemBackgroundBorderless"
            android:contentDescription="Editar inventario"
            android:src="@drawable/border_color_24px"
            android:scaleType="fitCenter"
            android:padding="8dp"
            app:tint="@color/dark_gray"
            tools:ignore="ImageContrastCheck" />
        <View
            android:layout_width="8dp"
            android:layout_height="match_parent" />
        <ImageButton
            android:id="@+id/button_add_collaborator"
            android:layout_width="48dp"
            android:layout_height="48dp"
            android:background="?attr/selectableItemBackgroundBorderless"
            android:contentDescription="Agregar colaborador"
            android:src="@drawable/groups_24px"
            android:scaleType="fitCenter"
            android:padding="8dp"
            app:tint="@color/purple_500"
            tools:ignore="ImageContrastCheck" />
        <View
            android:layout_width="8dp"
            android:layout_height="match_parent" />
        <ImageButton
            android:id="@+id/button_delete_inventory"
            android:layout_width="48dp"
            android:layout_height="48dp"
            android:background="?attr/selectableItemBackgroundBorderless"
            android:contentDescription="Eliminar inventario"
            android:src="@drawable/delete_24px"
            android:scaleType="fitCenter"
            android:padding="8dp"
            app:tint="@color/red"
            tools:ignore="ImageContrastCheck" />

    </LinearLayout>

    </androidx.constraintlayout.widget.ConstraintLayout>

</com.google.android.material.card.MaterialCardView>